/**
 * Hook to access data source context
 * Re-exports from context for convenience
 */

export { useDataSource } from '@/lib/data/data-source-context'
export type { DataSource, DataSourceContextValue } from '@/lib/data/data-source-context'
